# Secrets for CHAP and MS-CHAP authentication for pppd
# client        server        secret                IP addresses
# format: <username> <servername or *> <password> <allowed client IP or *>

"vpnuser"    *    "vpnpassword"    *
# Example for a specific server name if 'name = AndroidL2TPDServer' is important for matching:
# "vpnuser"    "AndroidL2TPDServer"    "vpnpassword"    *
